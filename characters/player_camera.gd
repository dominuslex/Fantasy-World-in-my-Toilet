extends Camera2D

var offset_direction = Vector2.ZERO

#func _process(delta):
#	offset_direction  self.get_parent().direction * (self.get_parent().TILE_SIZE * 2)
#	if offset != offset_direction:
#		offset = offset_direction
#
#	print(offset)
